import { style } from "@vanilla-extract/css";

export const productDetailFrame = style({
  display: "flex",
  flexDirection: "column",
  rowGap: 20,
  padding: "0 15px",
  marginBottom: 100
});

export const hrStyle = style({
  width: "100%",
});
